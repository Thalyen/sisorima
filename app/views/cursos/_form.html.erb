<%= form_for(@curso) do |f| %>
  <% if @curso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@curso.errors.count, "error") %> prohibited this curso from being saved:</h2>

      <ul>
      <% @curso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :duracao %><br>
    <%= f.number_field :duracao %>
  </div>
  <div class="field">
    <%= f.label :totalCredito %><br>
    <%= f.number_field :totalCredito %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
